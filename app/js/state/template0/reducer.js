import { Template0ActionTypes } from './actions'
const {  } = Template0ActionTypes

const INITIAL_STATE = {}

function Template0Reducer(state = INITIAL_STATE, action) {
  const { payload } = action
  switch (action.type) {

    default:
      return state;
  }
}

export { Template0Reducer }
